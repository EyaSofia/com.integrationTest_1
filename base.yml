#Automatically generated by the ConfigStore on the 2025-02-27T16:36:45.603532457
MyCompany\Tests\ORM\Mapping\User:
  type: entity
  repositoryClass: MyCompany\Repository\UserRepo
  table: app_users
  schema: app_schema
  readOnly: false
  indexes:
    user_name_index:
      columns: [ userName ]
  id:
    userId:
      type: uuid
      generator:
        strategy: UUID
  fields:
    userName:
      type: string
      length: 255
    contactEmail:
      type: string
      length: 128
      column: email_address
      unique: true
      options:
        fixed: false
        comment: "User's primary contact email"
    password:
      type: string
      length: 128
    isActive:
      type: boolean
      column: active_status
      nullable: false
      options:
        default: true
  oneToOne:
    profile:
      targetEntity: Profile
      joinColumn:
        name: profile_id
        referencedColumnName: id
        onDelete: SET NULL
  oneToMany:
    posts:
      targetEntity: Post
      mappedBy: author
      cascade: [ remove ]
  manyToMany:
    roles:
      targetEntity: Role
      joinTable:
        name: user_roles
        joinColumns:
          user_id:
            referencedColumnName: userId
        inverseJoinColumns:
          role_id:
            referencedColumnName: id
  lifecycleCallbacks:
    prePersist: [ initializeUser, validateEmail ]
    postUpdate: [ logUpdate ]
